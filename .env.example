# Configuración del Host
HOST_IP=your.ip.address            # Cambiar a la IP específica de tu máquina
DOMAIN=localhost                    # Cambiar al dominio si está disponible

# Puertos de los servicios
FRONTEND_PORT=5173
API_PORT=8000
TRANSPORT_PORT=9000
IMG_SERVER_PORT=9001

# Configuración de seguridad
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173  # Lista de orígenes permitidos
MAX_CONNECTIONS=100                 # Límite de conexiones simultáneas
RATE_LIMIT=100                     # Límite de peticiones por minuto
SESSION_TIMEOUT=3600               # Tiempo de expiración de sesión en segundos

# Configuración de SSL/TLS (recomendado para producción)
USE_HTTPS=true                     # Habilitado para conexiones seguras
SSL_CERT_PATH=./certs/cert.pem     # Ruta al certificado SSL
SSL_KEY_PATH=./certs/key.pem       # Ruta a la clave privada SSL

# Configuración de Ngrok para acceso remoto
NGROK_AUTHTOKEN=your_ngrok_token   # Tu token de autenticación de ngrok
NGROK_REGION=us                    # Región del servidor (us, eu, ap, au, sa, jp, in)